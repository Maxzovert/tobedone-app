# Optional override — the app auto-detects the API URL in dev:
# - Physical device: same IP as Expo Metro (QR code host)
# - Android emulator: http://10.0.2.2:3000
# - iOS simulator: http://localhost:3000
#
# Only set this if auto-detection fails:
# EXPO_PUBLIC_API_URL=http://192.168.29.183:3000
#
# If sign-in fails on a real phone, run as Admin:
#   mobile/scripts/allow-api-firewall.ps1
